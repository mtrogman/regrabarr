[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (mocked, no external dependencies)
    integration: Integration tests (requires live Sonarr/Radarr)
    slow: Slow running tests
asyncio_mode = auto
